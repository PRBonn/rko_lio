name: Python Bindings macOS 14
on:
  push:
    branches: [add_humble_support]
  pull_request:
    branches: [master]
jobs:
  python_bindings_macos_14:
    uses: ./.github/workflows/python_bindings_reusable.yaml
    with:
      run_os: macos-14
