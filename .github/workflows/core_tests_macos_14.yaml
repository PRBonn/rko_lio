name: Core C++ Tests macOS 14
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  core_tests_macos_14:
    uses: ./.github/workflows/core_tests_reusable.yaml
    with:
      run_os: macos-14
