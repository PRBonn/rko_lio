name: ROS Build Kilted
on:
  push:
    branches: [add_humble_support]
  pull_request:
    branches: [master]
jobs:
  build_kilted:
    uses: ./.github/workflows/ros_build_reusable.yaml
    with:
      ros_distro: kilted
