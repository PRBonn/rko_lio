name: ROS Build Jazzy
on:
  push:
    branches: [add_humble_support]
  pull_request:
    branches: [master]
jobs:
  build_jazzy:
    uses: ./.github/workflows/ros_build_reusable.yaml
    with:
      ros_distro: jazzy
